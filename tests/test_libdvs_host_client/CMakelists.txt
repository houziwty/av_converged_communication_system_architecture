cmake_minimum_required(VERSION 3.5...3.16)

project(test_libdvs_host_client VERSION "${BUILD_VERSION_STRING}" LANGUAGES CXX)

include_directories(${INCLUDE_DIR})

file(GLOB_RECURSE src "*.cpp")
file(GLOB_RECURSE framework_utils_src "${SOURCE_DIR}/utils/thread/*.cpp" "${SOURCE_DIR}/utils/time/*.cpp" "${SOURCE_DIR}/utils/url/*.cpp")

add_executable(${PROJECT_NAME} ${src} ${framework_utils_src})
target_link_libraries(${PROJECT_NAME} PRIVATE dvs_host_client)