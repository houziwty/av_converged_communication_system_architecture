cmake_minimum_required(VERSION 3.5...3.16)

project(test VERSION "${BUILD_VERSION_STRING}" LANGUAGES CXX)

include_directories(${SAMPLE_DIR}/libvideo_encode_image)

link_directories(${3RDPART_DIR}/sdk/hikvision/lib/linux)
link_directories(${3RDPART_DIR}/sdk/ffmpeg)

file(GLOB_RECURSE src "*.cpp")

add_executable(${PROJECT_NAME} ${src})
target_link_libraries(${PROJECT_NAME} PRIVATE video_encode_image)